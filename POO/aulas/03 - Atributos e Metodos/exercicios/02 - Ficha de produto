Uma loja de eletrônicos precisa representar seus produtos em código. Crie uma classe Produto que recebe, no construtor, as informações essenciais de um item e exibe uma ficha formatada.

O construtor deve receber: nome, preco e estoque

Implemente um método exibir_ficha(self) que imprime as três informações de forma legível

Crie dois produtos diferentes e chame exibir_ficha() em cada um

Bônus: adicione um método esta_disponivel(self) que retorna True se estoque > 0


Código base:
class Produto:
    def __init__(self, ???):
        self.nome    = ???
        self.preco   = ???
        self.estoque = ???

    def exibir_ficha(self):
        ???

    def esta_disponivel(self):
        ???

p1 = Produto("Teclado Mecânico", 349.90, 15)
p2 = Produto("Mouse Gamer", 189.00, 0)
p1.exibir_ficha()
p2.exibir_ficha()
print(p2.esta_disponivel())



dica
Use f-string para montar a ficha: f"Produto: {self.nome} | Preço: R$ {self.preco:.2f}". O :.2f formata com duas casas decimais.